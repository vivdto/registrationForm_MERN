step -1 : clone the source code
step -2 : run the npm install ( npm install )
step -3 : After downloading node_modules. run the node server
    cmd : npm run dev

url: http://localhost:3001 based on the npm serves
